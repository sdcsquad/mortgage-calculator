import React from 'react';
import styles from './styles/Button.css';

const Button = props => (
  <button type="button" className={styles.qualifiedButton}>Get pre-qualified</button>
);

export default Button;